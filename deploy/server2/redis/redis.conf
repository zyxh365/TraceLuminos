# TSP Redis 配置
bind 0.0.0.0
port 6379
timeout 300

# 持久化：RDB 快照（每 5 分钟至少 1 个 key 变化时触发）
save 300 1
save 60 10000

# 开启 AOF 持久化（更安全，写密集场景推荐）
appendonly yes
appendfsync everysec

# 内存限制
maxmemory 512mb
maxmemory-policy allkeys-lru

# 日志级别
loglevel notice
